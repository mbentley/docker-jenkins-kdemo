mbentley/jenkins-kdemo
==================

docker image for jenkins to use as a demo

To pull this image:
`docker pull mbentley/jenkins-kdemo`

Example usage:
`docker run -d -p 8080 mbentley/jenkins-kdemo`
